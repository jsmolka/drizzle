TEST_CASE("unary minus")
{
    // Todo: test type promotion
    //vmExec("assert -true == 0 - 1");
    //vmExec("assert -1 == 0 - 1");
    //vmExec("assert -1.0 == 0.0 - 1.0");
    //vmThrows<TypeError>("-null");
}

TEST_CASE("unary bitwise complement")
{
    // Todo: test type promotion
    //vmExec("assert ~true == -2");
    //vmExec("assert ~1 == -2");
    //vmThrows<TypeError>("~1.0");
    //vmThrows<TypeError>("~null");
}
