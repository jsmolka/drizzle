#pragma once

#include <shell/int.h>

using s8  = shell::s8;
using s16 = shell::s16;
using s32 = shell::s32;
using s64 = shell::s64;
using u8  = shell::u8;
using u16 = shell::u16;
using u32 = shell::u32;
using u64 = shell::u64;
